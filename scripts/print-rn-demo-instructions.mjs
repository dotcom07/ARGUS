#!/usr/bin/env node

console.log("ebay_argus RN is the primary demo artifact:");
console.log("- apps/marketplace-demo/src/App.tsx");
console.log("");
console.log("Install React Native dependencies in the target mobile workspace, then run the app with Metro/React Native CLI.");
console.log("The mobile-sized browser preview is still available with: npm run demo:web-preview");
