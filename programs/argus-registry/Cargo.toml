[package]
name = "argus-registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "argus_registry"

[dependencies]
anchor-lang = "0.32.1"

[features]
idl-build = ["anchor-lang/idl-build"]

[profile.release]
overflow-checks = true
